import { LoginForm } from '@/features/auth/components/LoginForm';

export default function LoginPage() {
  return (
    <div className="page">
      <h1 className="login-title">Pulse FX</h1>
      <LoginForm />
    </div>
  );
}
